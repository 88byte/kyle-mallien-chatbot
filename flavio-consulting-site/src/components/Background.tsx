/** Plain warm canvas. The design carries itself; the background stays quiet. */
export default function Background() {
  return <div className="pointer-events-none fixed inset-0 -z-10 bg-cream" />;
}
